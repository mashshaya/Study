from __future__ import annotations

import json
from pathlib import Path


PROJECT = Path("/Users/maria/Desktop/Code/HSE/ML_IN_EC/project")


def set_markdown(cell: dict, text: str) -> None:
    cell["source"] = [line + "\n" for line in text.strip("\n").split("\n")]


def set_code(cell: dict, text: str) -> None:
    lines = text.splitlines()
    cell["source"] = [line + "\n" for line in lines]
    if text and text[-1] != "\n":
        cell["source"][-1] = cell["source"][-1].rstrip("\n")


def update_02() -> None:
    path = PROJECT / "02_Генерация_данных.ipynb"
    nb = json.loads(path.read_text())
    cells = nb["cells"]

    set_markdown(
        cells[2],
        """
# Блок 2. Генерация и предварительная обработка данных

## Тема исследования

**Влияние премиум-подписки на сервис доставки на месячные траты клиента.**

Зависимая переменная: `monthly_spending` - месячные траты клиента в сервисе доставки.

Переменная воздействия: `premium_subscription` - оформил ли клиент премиум-подписку.

Ненаблюдаемая переменная: `online_preference` - склонность клиента к онлайн-покупкам. Она влияет и на вероятность оформить подписку, и на общий объем трат, поэтому создает эндогенность.

В этой симуляции мы сразу задаем экономическую историю, которая потом понадобится и для блока про эффекты воздействия:

- есть наблюдаемые характеристики клиента, которые влияют на подписку и траты;
- есть ненаблюдаемая склонность к онлайн-покупкам, которая тоже влияет и на подписку, и на расходы;
- есть инструмент `free_trial_banner`, который повышает вероятность подписки, но не должен напрямую менять траты, кроме как через решение оформить премиум.

## Контрольные переменные и их распределения

В модели используются следующие наблюдаемые контрольные переменные:

- `income` - доход клиента. Генерируется как логнормальная случайная величина: `Income = exp(N(11.1, 0.5^2))`.
- `age` - возраст клиента. Сначала генерируется как `N(35, 10^2)`, затем усекается снизу на уровне 18 и сверху на уровне 70, после чего округляется до целых лет.
- `urban_area` - бинарный индикатор проживания в крупном городе: `Bernoulli(0.7)`.
- `family_size` - размер семьи, принимающий значения от 1 до 5 с заранее заданными вероятностями.

Такой набор переменных соответствует содержательной задаче: доход отражает платежеспособность, возраст - стадию жизненного цикла, город - доступность сервиса доставки, размер семьи - потенциальный объем потребления.
        """,
    )

    set_code(
        cells[8],
        """online_preference_raw = norm.rvs(size = n, loc = 0, scale = 1)
# Преобразование через logistic.cdf переводит скрытую переменную на интервал (0, 1),
# что удобно для интерпретации, но само по себе не обязательно для эндогенности.
online_preference = logistic.cdf(online_preference_raw)""",
    )

    set_markdown(
        cells[10],
        """
## Инструментальная переменная

Инструмент `free_trial_banner` интерпретируется как факт того, что клиенту было показано маркетинговое предложение о бесплатном пробном периоде премиум-подписки.

Важно: в текущей симуляции это **не полностью случайный** инструмент. Вероятность показа зависит от наблюдаемых характеристик клиента. Это допустимо, если затем при интерпретации мы считаем, что после контроля по наблюдаемым характеристикам баннер влияет на траты только через подписку.

Математически генерация инструмента устроена так:

- сначала строится скрытый индекс `Z* = 1.5 * log(income * age) - 2 * urban_area / (6 - family_size) - 22`;
- затем вероятность показа задается как `P(Z = 1 | X) = Phi(Z*)`, где `Phi` - функция распределения стандартного нормального закона;
- после этого `free_trial_banner ~ Bernoulli(P(Z = 1 | X))`.

По построению такой инструмент должен быть релевантным: показ баннера повышает вероятность оформления подписки. Отдельно ниже мы проверяем это эмпирически через корреляцию и таблицы сопряженности.
        """,
    )

    set_code(
        cells[11],
        """free_trial_banner_index = 1.5*np.log(income*age) - 2*urban_area/(6-family_size)-22
# Формула индекса выбрана вручную и довольно агрессивно калибруется константой -22:
# если в других запусках поменяются параметры дохода/возраста, доля единиц может заметно "уплыть".
free_trial_banner_prob = norm.cdf(free_trial_banner_index)
free_trial_banner = np.random.binomial(n = 1, p = free_trial_banner_prob, size = n)""",
    )

    set_markdown(
        cells[14],
        """
## Генерация переменной воздействия

Переменная воздействия `premium_subscription` моделируется через потенциальные исходы для подписки.

Сначала задается базовый индекс склонности оформить подписку:

`premium_index = -16.8 + 12 * online_preference + 0.20 * log(income) + sqrt(age) + 0.35 * urban_area * family_size`.

Экономический смысл коэффициентов здесь такой:

- более высокая склонность к онлайн-покупкам повышает вероятность подписки;
- более высокий доход делает подписку более доступной;
- возраст влияет через нелинейный эффект `sqrt(age)`;
- комбинация `urban_area * family_size` усиливает спрос на премиум там, где доставка особенно удобна и домохозяйство потенциально больше потребляет.

Далее для каждого клиента строятся два потенциальных исхода:

- `premium1` - оформил бы подписку при наличии баннера;
- `premium0` - оформил бы подписку без баннера.

Наблюдаемое значение задается формулой выбора соответствующего потенциального исхода:

`premium_subscription = premium1 * free_trial_banner + premium0 * (1 - free_trial_banner)`.

Эта конструкция полезна дальше по двум причинам:

- она позволяет явно определить `Always taker`, `Never taker`, `Complier` и возможных `Defier`;
- она делает возможным расчет LATE на основе истинных потенциальных исходов.
        """,
    )

    set_code(
        cells[15],
        """u = uniform.rvs(size = n)
premium_index = -16.8 + 12 * online_preference + 0.20 * np.log(income) + np.sqrt(age) + 0.35 * urban_area * family_size
free_trial_banner1 = 1
premium1_prob = logistic.cdf(premium_index + 0.45 * free_trial_banner1)
premium1 = (u <= premium1_prob).astype(int)

free_trial_banner0 = 0
premium0_prob = logistic.cdf(premium_index + 0.45 * free_trial_banner0)
premium0 = (u <= premium0_prob).astype(int)

# Использование одного и того же u для premium1 и premium0 удобно:
# так автоматически обеспечивается монотонность premium1 >= premium0 и фактически исключаются defier.
# Это хорошая конструкция для IV-симуляции, но ее стоит держать в уме при интерпретации результатов.
premium_subscription = premium1 * free_trial_banner + premium0 * (1 - free_trial_banner)""",
    )

    set_markdown(
        cells[20],
        """
## Проверка ключевых связей

На этом шаге проверяются две важные связи, заложенные в симуляции:

- связь между `premium_subscription` и ненаблюдаемой переменной `online_preference` подтверждает наличие эндогенности;
- связь между `premium_subscription` и `free_trial_banner` подтверждает релевантность инструмента.

Это не доказательство корректности идентификации, а именно sanity check того, что код действительно генерирует задуманный механизм.
        """,
    )

    set_markdown(
        cells[22],
        """
## Генерация зависимой переменной

Зависимая переменная `monthly_spending` также строится через потенциальные исходы.

Мы задаем два возможных значения месячных трат:

- `monthly_spending0` - траты клиента без премиум-подписки;
- `monthly_spending1` - траты клиента при наличии премиум-подписки.

Каждый потенциальный исход складывается из трех частей:

- наблюдаемой компоненты, зависящей от дохода, возраста, города и размера семьи;
- ненаблюдаемой компоненты, зависящей от `online_preference`;
- случайной ошибки, распределение которой различается между режимами `D = 0` и `D = 1`.

Такой дизайн делает задачу содержательно интереснее простой линейной модели: эффект подписки не сводится к одной константе, а структура шумов и скрытых факторов остается нетривиальной.

После построения потенциальных исходов наблюдаемое значение определяется стандартно:

`monthly_spending = monthly_spending1 * premium_subscription + monthly_spending0 * (1 - premium_subscription)`.

Отрицательные значения трат затем обрезаются снизу нулем, чтобы зависимая переменная оставалась экономически интерпретируемой.
        """,
    )

    set_code(
        cells[24],
        """# error0 = t.rvs(size = n, df = 15) * 150
# error1 = expon.rvs(size = n, scale = 150) - 150

# Здесь ошибки специально сделаны разными по форме распределения.
# Это делает симуляцию богаче, но также усложняет прямое сопоставление режимов D=0 и D=1:
# часть различий между ними идет не только через средний эффект, но и через асимметрию/тяжелые хвосты.
error0 = t.rvs(size = n, df = 15) * 1500
error1 = expon.rvs(size = n, scale = 1500) - 1500""",
    )

    set_code(
        cells[27],
        """monthly_spending0 = g0 + error0
monthly_spending1 = g1 + error1
monthly_spending0[monthly_spending0 <= 0] = 0
monthly_spending1[monthly_spending1 <= 0] = 0

# Обрезка снизу на нуле экономически понятна, но создает массу точек в нуле.
# Из-за этого некоторые метрики вроде MAPE в регрессии потом потребуют аккуратной оговорки.
monthly_spending = monthly_spending1 * premium_subscription + monthly_spending0 * (1 - premium_subscription)""",
    )

    set_markdown(
        cells[28],
        """
## Проверка адекватности симуляции

Ниже проверяется, что дисперсии случайных ошибок и структурных частей модели находятся в сопоставимых масштабах. Это нужно, чтобы зависимая переменная не определялась почти полностью ни шумом, ни одной-единственной компонентой.

Отдельно это соответствует замечанию из консультации: дисперсии разных составляющих не должны различаться слишком сильно.
        """,
    )

    set_markdown(
        cells[32],
        """
## Итоговый датасет и описательные проверки

После генерации все наблюдаемые переменные объединяются в датафрейм `df`.

Затем выполняются базовые проверки, которые требуются во втором блоке задания:

- описательные статистики по непрерывным переменным;
- визуальная проверка распределений;
- проверка долей бинарных переменных;
- сопоставление фактического распределения `family_size` с целевыми вероятностями;
- проверка того, насколько инструмент меняет вероятность подписки;
- расчет корреляционной матрицы;
- разбиение на обучающую и тестовую выборки.

Эти шаги одновременно помогают проверить корректность симуляции и подготовить данные для следующих ноутбуков.
        """,
    )

    set_markdown(
        cells[48],
        """
# <font color="blue">корреляционная матрица</font>

Матрица корреляций ниже нужна не для причинной интерпретации, а для первичной диагностики структуры данных. По ней удобно увидеть, связаны ли:

- инструмент и воздействие;
- воздействие и итоговые траты;
- наблюдаемые контрольные переменные между собой.
        """,
    )

    set_markdown(
        cells[50],
        """
# <font color="blue">трэйнтестсплит</font>

Выборка делится на обучающую и тестовую в пропорции 75/25. Это соответствует условию задания, где тестовая часть должна составлять от 20% до 30% наблюдений.

Далее сохраняются `df`, `df_train` и `df_test`, чтобы следующие блоки можно было воспроизводить на тех же данных.
        """,
    )

    path.write_text(json.dumps(nb, ensure_ascii=False, indent=1))


def update_03() -> None:
    path = PROJECT / "03_Классификация.ipynb"
    nb = json.loads(path.read_text())
    cells = nb["cells"]

    set_markdown(
        cells[9],
        """
## <font color="blue">подготовка признаков для логита</font>

В этом ноутбуке мы решаем задачу прогнозирования бинарной переменной `premium_subscription`.

Для логистической регрессии используем стандартизацию признаков, потому что:

- коэффициенты логита оцениваются численно;
- признаки измеряются в разных шкалах;
- без нормализации оптимизация и интерпретация могут быть менее устойчивыми.

При этом важно, что для кросс-валидации и `GridSearchCV` scaling будет выполняться **внутри** `Pipeline`, чтобы избежать утечки данных между фолдами.

Для случайного леса и градиентного бустинга оставляем исходные признаки без нормализации: деревьям масштаб признаков обычно не мешает.
        """,
    )

    set_code(
        cells[8],
        """features_train, features_test, target_train, target_test = train_test_split(
    features, target, test_size = 0.25, random_state = 123)

# Здесь выборка режется заново, а не используются сохраненные df_train/df_test из предыдущего ноутбука.
# При текущем порядке строк и том же random_state разбиение должно совпасть,
# но при любом изменении порядка данных эта синхронизация может потеряться.
n_train = len(target_train)
n_test  = len(target_test)
print({'n_train': n_train, 'n_test': n_test})""",
    )

    set_markdown(
        cells[12],
        """
## <font color="blue">логит</font>

Сначала оцениваем логистическую регрессию с фиксированными гиперпараметрами. Это базовая интерпретируемая модель, которая задает линейную зависимость логарифма шансов подписки от признаков.

Здесь считаются три показателя качества:

- `ACC train` - качество на обучающей выборке;
- `ACC test` - качество на отложенной тестовой выборке;
- `ACC CV` - среднее качество по кросс-валидации на train.

Сравнение этих трех величин потом поможет обсуждать переобучение.
        """,
    )

    set_markdown(
        cells[15],
        """
## <font color="blue">случайный лес</font>

Случайный лес нужен как более гибкая нелинейная альтернатива логиту. Он может автоматически ловить взаимодействия между признаками и пороговые эффекты без явного задания функциональной формы.

Как и в семинарах, сравниваем качество на train, test и по CV.
        """,
    )

    set_markdown(
        cells[18],
        """
## <font color="blue">градиентный бустинг</font>

Градиентный бустинг по деревьям строит ансамбль последовательно: каждое новое дерево пытается улучшить ошибки предыдущих. В задачах классификации он часто оказывается сильным конкурентом случайному лесу, особенно если важны сложные нелинейности.
        """,
    )

    set_markdown(
        cells[21],
        """
# <font color="blue">тюнинг гиперпараметров по accuracy</font>

После базового сравнения подбираем гиперпараметры по `accuracy` только на обучающей выборке.

Логика ровно такая же, как в семинарских ноутбуках:

- задаем решетку параметров;
- запускаем `GridSearchCV` на train;
- выбираем комбинацию с лучшим средним качеством по фолдам;
- затем отдельно оцениваем полученную модель на тесте.

Для логита снова используется `Pipeline`, чтобы стандартизация не нарушала чистоту кросс-валидации.
        """,
    )

    set_markdown(
        cells[31],
        """
## <font color="blue">сводная таблица (accuracy)</font>

В этой таблице удобно смотреть сразу на несколько вещей:

- какие гиперпараметры были заданы изначально;
- какие параметры выбраны после CV-тюнинга;
- улучшилось ли качество на тесте;
- нет ли большого разрыва между train-, CV- и test-метриками.

Именно по таким сопоставлениям потом можно аргументированно обсуждать лучшую и худшую модель.
        """,
    )

    set_markdown(
        cells[33],
        """
## <font color="blue">OOB для RF (повышенная сложность)</font>

Для случайного леса дополнительно считаем `OOB`-ошибку. Это аналог внутренней валидации для бэггинга: каждое наблюдение прогнозируется теми деревьями, которые не видели его при обучении.

Содержательно этот блок нужен, чтобы сравнить:

- насколько `OOB` согласуется с обычной CV;
- можно ли использовать его как более дешевую альтернативу кросс-валидации для леса.
        """,
    )

    set_code(
        cells[34],
        """rf_oob = RandomForestClassifier(max_depth    = 12,
                                max_features = "sqrt",
                                max_samples  = 500,
                                random_state = 777,
                                criterion    = 'entropy',
                                oob_score    = True)
# При max_samples=500 каждое дерево обучается лишь на небольшой подвыборке из train.
# Это допустимо и похоже на семинарский стиль, но OOB и итоговое качество тут чувствительны к этой настройке.
rf_oob.fit(features_train_raw, target_train)
oob_rf = 1 - rf_oob.oob_score_

rf_oob2 = deepcopy(rf_oob)
rf_oob2.max_features = None
rf_oob2.max_depth    = 8
rf_oob2.fit(features_train_raw, target_train)
oob_rf2 = 1 - rf_oob2.oob_score_

print(pd.DataFrame(data    = [oob_rf, oob_rf2],
                   index   = ['RF 1 (исходный)', 'RF 2 (другие гиперпараметры)'],
                   columns = ['OOB']))""",
    )

    set_markdown(
        cells[38],
        """
# <font color="blue">3.4 ROC и AUC</font>

После сравнения по `accuracy` полезно перейти к качеству ранжирования вероятностей.

- ROC-кривая показывает компромисс между `TPR` и `FPR` при изменении порога.
- `AUC` агрегирует это качество в одно число и не зависит от конкретного выбранного порога.

Это особенно важно здесь, потому что дальше порог будет подбираться уже из экономических соображений.
        """,
    )

    set_markdown(
        cells[41],
        """
# <font color="blue">3.5 матрица ошибок (gb, порог 0.5)</font>

Для градиентного бустинга отдельно выписываем матрицу ошибок при стандартном пороге `0.5`. Это помогает увидеть структуру ошибок модели, а не только одну сводную метрику.
        """,
    )

    set_markdown(
        cells[45],
        """
# <font color="blue">3.7 оптимальный порог по прибыли</font>

Здесь фокус смещается с статистической точности на управленческое решение.

Мы считаем не общую выручку клиентов, а **дополнительную прибыль от использования модели**. Для каждого порога:

- получаем бинарное решение;
- раскладываем прогнозы на `TP`, `TN`, `FP`, `FN`;
- присваиваем каждому типу исхода денежный выигрыш или убыток;
- выбираем порог, максимизирующий прибыль на обучающей выборке.

После этого лучший для train порог переносится на test, и уже там сравниваются фактические прибыли моделей.
        """,
    )

    set_code(
        cells[47],
        """# Сюжет: тем, кому модель прогнозирует НЕподписку (predicted=0), отправляется
# промокод на бесплатный пробный период премиум-подписки.
# Стоимость рассылки промокода = 500 руб. Клиент, который после промокода оформляет подписку,
# приносит дополнительную прибыль 2500 руб.

# Ниже важно не запутаться в знаках: действие применяется именно к predicted=0,
# поэтому экономический смысл TP/TN/FP/FN здесь не совсем стандартен относительно
# обычной классификационной интерпретации "класс 1 = таргетируем".
prices = pd.Series({'TP': 0,
                    'TN': 2000,
                    'FP': -500,
                    'FN': 0})""",
    )

    set_code(
        cells[49],
        """def best_threshold_profit(model, features_train, target_train, features_test, target_test, prices):
    prob_train = model.predict_proba(features_train)[:, 1]
    thresholds = np.unique(np.sort(prob_train))
    # Перебор всех уникальных train-вероятностей дает точный максимум на сетке,
    # но на больших данных может быть вычислительно тяжелее, чем редкая сетка порогов.
    profits_train = profits_thresholds(model, features_train, target_train, thresholds, prices)
    c_opt = thresholds[np.argmax(profits_train)]
    prob_test = model.predict_proba(features_test)[:, 1]
    pred_test = (prob_test >= c_opt).astype(int)
    TN, FP, FN, TP = confusion_matrix(target_test, pred_test).ravel()
    predictions = pd.Series([TP, TN, FP, FN], index = ["TP", "TN", "FP", "FN"])
    profit_test = np.sum(prices * predictions)
    return c_opt, profit_test""",
    )

    set_markdown(
        cells[51],
        """
# <font color="blue">3.8 DAG (содержательный)</font>

Здесь классификационная часть связывается с причинной логикой проекта.

Сначала вручную задается содержательный DAG: какие переменные, по нашему замыслу, влияют на `premium_subscription` и как между собой могут быть связаны наблюдаемые факторы. Затем по этому графу обучается Байесовская сеть и строится прогноз.

После этого структура сети оценивается уже автоматически по данным, и сравнивается точность:

- пользовательского DAG, основанного на экономической логике;
- обученного DAG, найденного алгоритмом.
        """,
    )

    set_code(
        cells[55],
        """import bnlearn

# Биннинг непрерывных признаков нужен, потому что далее используется дискретная Байесовская сеть.
# qcut удобен, но при большом числе повторов или узком диапазоне может дать не совсем симметричные группы.
df_bn = df.copy()
df_bn['income_bin'] = pd.qcut(df_bn['income'], q=3, labels=[0, 1, 2]).astype(int)
df_bn['age_bin']    = pd.qcut(df_bn['age'],    q=3, labels=[0, 1, 2], duplicates='drop').astype(int)

bn_cols = ['income_bin', 'age_bin', 'urban_area', 'family_size',
           'free_trial_banner', 'premium_subscription']
df_bn = df_bn[bn_cols].astype(int)

# Для скорости берём подвыборку 10k наблюдений.
# Это ускоряет bnlearn, но результаты DAG и ACC уже относятся не ко всей выборке, а к случайной подвыборке.
df_bn_sub = df_bn.sample(n=10000, random_state=123).reset_index(drop=True)
df_bn_train, df_bn_test = train_test_split(df_bn_sub, test_size=0.25, random_state=123)
df_bn_train = df_bn_train.reset_index(drop=True)
df_bn_test  = df_bn_test.reset_index(drop=True)""",
    )

    set_markdown(
        cells[60],
        """
# <font color="blue">3.9 лучший / худший классификатор</font>

В качестве итогового критерия здесь выбирается `AUC` на тестовой выборке. Это разумно, потому что:

- `AUC` оценивает качество ранжирования вероятностей;
- метрика не зависит от одного фиксированного порога;
- далее в прикладной задаче порог все равно выбирается отдельно под прибыль.

Поэтому лучшей считается модель с максимальным `AUC`, а худшей - модель с минимальным.
        """,
    )

    set_markdown(
        cells[63],
        """
# <font color="blue">диагностика похожих результатов</font>

Этот блок нужен как проверка на случай, когда разные модели дают почти одинаковые числа и возникает вопрос: это действительно свойство данных или ошибка в коде?

Ниже проверяются четыре возможных объяснения:

- сильный дисбаланс целевой переменной;
- почти полное совпадение бинарных прогнозов;
- почти одинаковые предсказанные вероятности;
- доминирование одного очень сильного признака.
        """,
    )

    set_markdown(
        cells[65],
        """
## <font color="blue">сравнение бинарных прогнозов</font>

Если модели почти всегда относят одного и того же клиента к одному и тому же классу, то близкие значения `accuracy` не должны удивлять: модели фактически принимают одинаковые решения.
        """,
    )

    set_markdown(
        cells[67],
        """
## <font color="blue">сравнение вероятностей</font>

Даже когда бинарные решения совпадают, полезно проверить, одинаково ли модели ранжируют клиентов по вероятности подписки. Именно это важно для ROC/AUC и для последующей оптимизации порога по прибыли.
        """,
    )

    set_markdown(
        cells[69],
        """
## <font color="blue">роль признаков</font>

Если один признак почти целиком объясняет вероятность подписки, то сложные методы могут вести себя очень похоже. Для деревьев это удобно смотреть через `feature_importance`, а для всей задачи в целом это еще и содержательная подсказка о том, насколько сильным получился инструмент.
        """,
    )

    set_markdown(
        cells[71],
        """
## <font color="blue">точность по одному сильному признаку</font>

Финальная проверка в этом разделе: насколько далеко можно уехать, если использовать только `free_trial_banner`. Если даже такой наивный прогнозор уже дает неплохую точность, то почти совпадающие результаты сложных моделей становятся вполне объяснимыми.
        """,
    )

    path.write_text(json.dumps(nb, ensure_ascii=False, indent=1))


def update_04() -> None:
    path = PROJECT / "04_Регрессия.ipynb"
    nb = json.loads(path.read_text())
    cells = nb["cells"]

    set_code(
        cells[8],
        """features_train, features_test, target_train, target_test = train_test_split(
    features, target, test_size = 0.25, random_state = 123)

# Как и в классификации, train/test формируется заново из df, а не читается из df_train/df_test.
# Пока порядок строк не менялся, разбиение должно совпасть с блоком генерации данных,
# но это неявная зависимость, о которой полезно помнить.
n_train = len(target_train)
n_test  = len(target_test)
print({'n_train': n_train, 'n_test': n_test})""",
    )

    set_markdown(
        cells[9],
        """
## <font color="blue">подготовка признаков</font>

В этом ноутбуке прогнозируется непрерывная переменная `monthly_spending`.

Выбраны три метода из списка задания:

- МНК как базовая линейная модель;
- случайный лес как нелинейный ансамблевый метод;
- градиентный бустинг как более гибкий последовательный ансамбль.

KNN здесь не используется, поэтому дополнительная нормализация признаков не нужна. Это также упрощает кросс-валидацию: все модели ниже обучаются на исходных признаках без отдельного preprocessing pipeline.
        """,
    )

    set_markdown(
        cells[11],
        """
## <font color="blue">функции метрик</font>

Для регрессии считаются две метрики:

- `RMSE` - измеряет типичный масштаб ошибки в тех же единицах, что и `monthly_spending`;
- `MAPE` - показывает относительную ошибку прогноза.

Так как в данных после обрезки есть нулевые траты, `MAPE` приходится считать аккуратно: наблюдения с `y = 0` исключаются из знаменателя.
        """,
    )

    set_code(
        cells[12],
        """from sklearn.metrics import make_scorer

def rmse(y_true, y_pred):
    return np.sqrt(metrics.mean_squared_error(y_true, y_pred))

def mape(y_true, y_pred):
    y_true = np.asarray(y_true); y_pred = np.asarray(y_pred)
    mask = y_true != 0
    # Из-за нулевых трат MAPE здесь считается только по ненулевым наблюдениям.
    # Это практичное решение, но метрика уже описывает не всю выборку целиком.
    if mask.sum() == 0:
        return np.nan
    return np.mean(np.abs((y_true[mask] - y_pred[mask]) / y_true[mask]))

mape_scorer = make_scorer(mape, greater_is_better=False)""",
    )

    set_markdown(
        cells[13],
        """
# <font color="blue">4.2 модели с исходными гиперпараметрами</font>

Сначала оцениваем три выбранные модели с фиксированными гиперпараметрами.

Для каждой модели сравниваются:

- качество на обучающей выборке;
- качество на тестовой выборке;
- качество по кросс-валидации на train.

Такое сопоставление позволяет увидеть, есть ли признаки переобучения еще до тюнинга гиперпараметров.
        """,
    )

    set_markdown(
        cells[14],
        """
## <font color="blue">МНК</font>

Линейная регрессия выступает как базовая параметрическая модель. На ее фоне удобно понимать, действительно ли нелинейные ансамбли дают содержательный прирост качества.
        """,
    )

    set_markdown(
        cells[17],
        """
## <font color="blue">случайный лес</font>

Случайный лес позволяет автоматически учитывать нелинейности и взаимодействия признаков. Если зависимость трат от факторов сложнее линейной, здесь можно ожидать улучшения относительно МНК.
        """,
    )

    set_markdown(
        cells[20],
        """
## <font color="blue">градиентный бустинг</font>

Градиентный бустинг по деревьям строит модель последовательно, исправляя ошибки предыдущих итераций. На задачах регрессии он часто дает более точную аппроксимацию, но может быть чувствителен к гиперпараметрам и переобучению.
        """,
    )

    set_markdown(
        cells[23],
        """
# <font color="blue">тюнинг гиперпараметров</font>

Далее подбираем гиперпараметры по кросс-валидации на обучающей выборке. Основной критерий тюнинга здесь - `RMSE`, потому что он измеряется в тех же единицах, что и зависимая переменная, и напрямую интерпретируется как типичный размер ошибки.
        """,
    )

    set_markdown(
        cells[24],
        """
## <font color="blue">МНК (без гиперпараметров)</font>

У линейной регрессии в данной постановке нет существенного тюнинга, поэтому итоговая модель совпадает с исходной. Этот блок нужен скорее для симметрии структуры раздела и для последующего сопоставления с ансамблями.
        """,
    )

    set_markdown(
        cells[26],
        """
## <font color="blue">RF (тюнинг)</font>

Для случайного леса перебираем глубину деревьев, число деревьев и способ выбора признаков. После поиска лучшей комбинации отдельно считаем итоговые `RMSE` и `MAPE` на тестовой выборке.
        """,
    )

    set_code(
        cells[28],
        """rfr2 = RandomForestRegressor(max_depth    = best_rfr_params['max_depth'],
                             max_features = best_rfr_params['max_features'],
                             n_estimators = best_rfr_params['n_estimators'],
                             random_state = 777,
                             criterion    = 'squared_error')
rfr2.fit(features_train_raw, target_train)
RMSE_test_rfr2 = rmse(target_test, rfr2.predict(features_test_raw))
MAPE_test_rfr2 = mape(target_test, rfr2.predict(features_test_raw))
RMSE_CV_rfr2 = -GSCV_rfr.best_score_
# MAPE после тюнинга досчитывается отдельным cross_val_score, потому что GridSearch шел по RMSE.
MAPE_CV_rfr2 = -np.mean(cross_val_score(rfr2, features_train_raw, target_train,
                                        cv = 5, scoring = mape_scorer))""",
    )

    set_markdown(
        cells[29],
        """
## <font color="blue">GB (тюнинг)</font>

Для бустинга подбираем глубину деревьев, `learning_rate` и число итераций. Это ключевые параметры, определяющие компромисс между гибкостью модели, скоростью обучения и риском переобучения.
        """,
    )

    set_code(
        cells[31],
        """gbr2 = GradientBoostingRegressor(loss = 'squared_error',
                                 n_estimators = best_gbr_params['n_estimators'],
                                 learning_rate = best_gbr_params['learning_rate'],
                                 max_depth = best_gbr_params['max_depth'],
                                 random_state = 123)
gbr2.fit(features_train_raw, target_train)
RMSE_test_gbr2 = rmse(target_test, gbr2.predict(features_test_raw))
MAPE_test_gbr2 = mape(target_test, gbr2.predict(features_test_raw))
RMSE_CV_gbr2 = -GSCV_gbr.best_score_
# Аналогично лесу: RMSE приходит из GridSearchCV, а MAPE считаем отдельным прогоном CV.
MAPE_CV_gbr2 = -np.mean(cross_val_score(gbr2, features_train_raw, target_train,
                                        cv = 5, scoring = mape_scorer))""",
    )

    set_markdown(
        cells[32],
        """
## <font color="blue">сводная таблица</font>

Сводим в одну таблицу:

- исходные и подобранные гиперпараметры;
- CV-метрики до и после тюнинга;
- test-метрики до и после тюнинга.

По этой таблице удобно смотреть, улучшает ли тюнинг качество и не сопровождается ли это ростом разрыва между train/CV/test.
        """,
    )

    set_markdown(
        cells[34],
        """
# <font color="blue">4.3 лучшая / худшая модель</font>

В качестве основного критерия выбора используется `RMSE` на тестовой выборке. Это естественный выбор для регрессии, потому что:

- метрика выражена в рублях, то есть в понятных единицах;
- она чувствительна к крупным ошибкам прогноза;
- именно вневыборочное качество отражает практическую полезность модели.
        """,
    )

    path.write_text(json.dumps(nb, ensure_ascii=False, indent=1))


if __name__ == "__main__":
    update_02()
    update_03()
    update_04()
